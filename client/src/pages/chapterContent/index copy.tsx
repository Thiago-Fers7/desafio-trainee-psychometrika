import { useLocation } from "react-router-dom"

function NoMatcah() {
    // const location = useLocation<Location>()

    return (
        <h3>Pagina não encontrada {'oi'}</h3>
    )
}

export { NoMatcah }