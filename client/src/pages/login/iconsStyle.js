import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
    root: {
        color: '#5c939d',
        position: 'absolute',
        bottom: '2.4rem',
        right: '1rem',
        fontSize: '1.6rem',
        transform: 'translateY(50%)',
        cursor: 'pointer'
    },
})

export { useStyles }